{
  "name": "sulu_demo",
  "version": "1.0.51",
  "private": true,
  "description": "Basic bundle structure for a default website project.",
  "author": "Sulu GmbH",
  "license": "ISC",
  "scripts": {
    "info": "npm-scripts-info",
    "build": "npm run build:css && npm run build:js && npm run update:version",
    "watch": "concurrently 'npm run watch:js' 'npm run watch:css'",
    "lint": "npm run lint:css && npm run lint:js",
    "lint:fix": "npm run lint:css:fix && npm run lint:js:fix",
    "build:js": "npm run lint:js:fix && npm run webpack",
    "watch:js": "webpack js/main.js -w -o ../../public/build/website/js/main.js --module-bind js=babel-loader --mode=development",
    "webpack": "webpack js/main.js -o ../../public/build/website/js/main.js --module-bind js=babel-loader -p --display-modules --sort-modules-by size",
    "lint:js": "eslint js",
    "lint:js:fix": "eslint js --fix",
    "build:css": "npm run lint:css:fix && npm run scss-autoprefix && npm run cssnano",
    "watch:css": "onchange 'css/**/*.scss' 'css/*.scss' -- npm run scss-autoprefix",
    "scss": "node-sass --output-style expanded -o ../../public/build/website/css css",
    "scss-autoprefix": "npm run scss && npm run autoprefixer",
    "autoprefixer": "postcss -u autoprefixer -r ../../public/build/website/css/main.css --no-map",
    "cssnano": "postcss -u cssnano -r ../../public/build/website/css/main.css --no-map",
    "lint:css": "stylelint css/*.scss css/**/*.scss",
    "lint:css:fix": "stylelint css/*.scss css/**/*.scss --fix",
    "update:version": "npm version patch --git-tag-version false"
  },
  "scripts-info": {
    "build": "Compiles the js and css, checks the code style and update the build version.",
    "watch": "Watch the js and css changes.",
    "lint": "Check the js and css code style.",
    "lint:fix": "Check the js and css code style and tries to fix it",
    "build:js": "Compiles js and checks the js code style.",
    "watch:js": "Compiles js and watches for changes.",
    "lint:js": "Checks the js code style.",
    "lint:js:fix": "Checks the js code style and tries to fix it.",
    "build:css": "Compiles css and checks the css code style.",
    "watch:css": "Compiles css and watches for changes.",
    "lint:css": "Checks the css code style.",
    "lint:css:fix": "Checks the css code style and tries to fix it.",
    "update:version": "Update the build version which is used for static assets."
  },
  "dependencies": {
    "@sulu/web": "^2.0.0-rc3",
    "core-js": "^3.2.1",
    "jquery": "^3.4.1",
    "normalize.css": "^8.0.1",
    "slick-carousel": "^1.8.1",
    "timeago.js": "^4.0.1"
  },
  "devDependencies": {
    "@babel/core": "^7.6.3",
    "@babel/plugin-proposal-class-properties": "^7.7.0",
    "@babel/preset-env": "^7.6.3",
    "autoprefixer": "^9.6.4",
    "babel-eslint": "^10.0.3",
    "babel-loader": "^8.0.6",
    "concurrently": "^5.0.0",
    "cssnano": "^4.1.10",
    "eslint": "^6.5.1",
    "eslint-config-ma": "^1.1.0",
    "node-sass": "^4.14.1",
    "npm-scripts-info": "^0.3.9",
    "onchange": "^6.1.0",
    "postcss-cli": "^7.1.2",
    "stylelint": "^13.7.0",
    "stylelint-config-ma": "^1.1.4",
    "stylelint-order": "^3.1.1",
    "stylelint-scss": "^3.11.1",
    "webpack": "^4.41.0",
    "webpack-cli": "^3.3.9"
  },
  "browserslist": [
    "> 1% in alt-EU",
    "last 2 versions"
  ],
  "engines": {
    "node": "^12.0"
  }
}

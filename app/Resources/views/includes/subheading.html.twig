<div rel="blocks" typeof="block">
    <div property="subheading" class="subheading">
        {% if block.subheading|default('') %}
            {{ block.subheading|raw }}
        {% endif %}
    </div>
</div>